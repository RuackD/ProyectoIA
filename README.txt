Para compilar utilizar en la terminal:
make compile
Para borrar los archivos compilados:
make clean 

Para correr el programa usar en el directorio del proyecto:

./main Instancias/airlandX.txt

Donde X representa el numero de la instancia, que va de 1 a 13.